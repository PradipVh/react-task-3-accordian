import React from 'react'
import { RouterProvider } from 'react-router-dom'
import { router } from './Router/Router'

function TAsk55Main() {
  return (
    <div>
      <RouterProvider router={router} />
    </div>
  )
}

export default TAsk55Main